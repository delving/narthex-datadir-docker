sudo: required

services:
  - docker

script:
  - docker build -t delvingplatform/narthex-data-initial .


after_success:
  - bash <(curl -s https://codecov.io/bash)
  - docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD";
    docker push delvingplatform/narthex;